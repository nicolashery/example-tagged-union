openapi: 3.0.0
info:
  title: Directory API
  version: 1.0.0
paths:
  /operations:
    post:
      summary: Add operation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Operation"
      responses:
        "201":
          description: Operation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Operation"
    get:
      summary: List operations
      responses:
        "200":
          description: A list of operations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListOperationsResponse"
components:
  schemas:
    ListOperationsResponse:
      type: object
      properties:
        operations:
          type: array
          items:
            $ref: "#/components/schemas/Operation"
    Operation:
      type: object
      discriminator:
        propertyName: type
        mapping:
          "create_object": "#/components/schemas/CreateObjectOperation"
          "update_object": "#/components/schemas/UpdateObjectOperation"
          "delete_object": "#/components/schemas/DeleteObjectOperation"
          "delete_all_objects": "#/components/schemas/DeleteAllObjectsOperation"
          "create_relation": "#/components/schemas/CreateRelationOperation"
          "delete_relation": "#/components/schemas/DeleteRelationOperation"
          "delete_all_relations": "#/components/schemas/DeleteAllRelationsOperation"
      oneOf:
        - $ref: "#/components/schemas/CreateObjectOperation"
        - $ref: "#/components/schemas/UpdateObjectOperation"
        - $ref: "#/components/schemas/DeleteObjectOperation"
        - $ref: "#/components/schemas/DeleteAllObjectsOperation"
        - $ref: "#/components/schemas/CreateRelationOperation"
        - $ref: "#/components/schemas/DeleteRelationOperation"
        - $ref: "#/components/schemas/DeleteAllRelationsOperation"
    CreateObjectOperationWrapper:
      type: object
      properties:
        type:
          type: string
        value:
          $ref: "#/components/schemas/CreateObjectOperation"
    UpdateObjectOperationWrapper:
      type: object
      properties:
        type:
          type: string
        value:
          $ref: "#/components/schemas/UpdateObjectOperation"
    DeleteObjectOperationWrapper:
      type: object
      properties:
        type:
          type: string
        value:
          $ref: "#/components/schemas/DeleteObjectOperation"
    CreateRelationOperationWrapper:
      type: object
      properties:
        type:
          type: string
        value:
          $ref: "#/components/schemas/CreateRelationOperation"
    DeleteRelationOperationWrapper:
      type: object
      properties:
        type:
          type: string
        value:
          $ref: "#/components/schemas/DeleteRelationOperation"
    CreateObjectOperation:
      type: object
      properties:
        type:
          type: string
        object:
          $ref: "#/components/schemas/DirectoryObject"
    UpdateObjectOperation:
      type: object
      properties:
        type:
          type: string
        object:
          $ref: "#/components/schemas/DirectoryObject"
    DeleteObjectOperation:
      type: object
      properties:
        type:
          type: string
        objectType:
          $ref: "#/components/schemas/ObjectType"
        objectId:
          type: string
    DeleteAllObjectsOperation:
      type: object
      properties:
        type:
          type: string
    CreateRelationOperation:
      type: object
      properties:
        type:
          type: string
        relation:
          $ref: "#/components/schemas/DirectoryRelation"
    DeleteRelationOperation:
      type: object
      properties:
        type:
          type: string
        relation:
          $ref: "#/components/schemas/DirectoryRelation"
    DeleteAllRelationsOperation:
      type: object
      properties:
        type:
          type: string
    DirectoryObject:
      type: object
      properties:
        type:
          $ref: "#/components/schemas/ObjectType"
        id:
          type: string
        properties:
          type: object
          additionalProperties:
            type: string
    DirectoryRelation:
      type: object
      properties:
        objectType:
          $ref: "#/components/schemas/ObjectType"
        objectId:
          type: string
        relation:
          type: string
        subjectType:
          $ref: "#/components/schemas/ObjectType"
        subjectId:
          type: string
    ObjectType:
      type: string
      enum: ["user", "group"]
