linters:
  enable:
    - exhaustive
    - gochecksumtype
    - exhaustruct
linters-settings:
  exhaustive:
    check:
      - switch
      - map
  gochecksumtype:
    default-signifies-exhaustive: false
  exhaustruct:
    exclude:
      - 'example/union/alt1\.Action'
      - 'example/union/alt2\.Action'
